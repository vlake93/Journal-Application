<h1>Task</h1>
<%= link_to "New task", new_user_category_task_path(tasks, @category) %>

<% tasks.each do |task| %>
  <div class="task">
    <h2><%= task.name %></h2>
    <h2><%= task.details %></h2>
    <h2><%= task.date %></h2>
  </div>
<% end %>